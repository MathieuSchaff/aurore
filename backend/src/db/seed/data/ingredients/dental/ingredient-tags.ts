import type { IngredientTagMap } from '../../ingredient-tags'

// Skeleton: dental ingredient→tag associations removed (live in the SQL
// snapshot). Add entries back keyed by ingredient slug to seed them from TS.
export const dentalTagMap: IngredientTagMap = {}
